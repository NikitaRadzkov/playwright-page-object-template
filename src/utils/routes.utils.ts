const routes = {
  page: '/page',
  resources: '/resources',
  layout: '/layout',
  signup: '/signup',
  automations: '/automations',
  projects: '/projects',
  register: '/register',
  login: '/login'
} as const;

export default routes;
